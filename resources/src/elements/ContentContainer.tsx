import styled from '@emotion/styled';
import tw from 'twin.macro';

const ContentContainer = styled.div`
    ${tw`relative flex flex-col w-full h-full mt-24 p-4`}
`;

export default ContentContainer;
