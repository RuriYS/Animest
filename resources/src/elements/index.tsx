import { lazy } from 'react';

export const Constraint = lazy(() => import('./Constraint'));
export const MainContainer = lazy(() => import('./MainContainer'));
export const ContentContainer = lazy(() => import('./ContentContainer'));
